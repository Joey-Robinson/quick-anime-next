import Navigation from "./header.nav"
import Notification from "./header.notification"

const Header = () => {
  return (
    <header>
      <Navigation />
      <Notification />
    </header>
  )
}

export default Header
